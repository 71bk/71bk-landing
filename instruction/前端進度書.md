# 71BK Frontend 進度書

> **用途**：前端開發進度追蹤，用於與後端溝通協調  
> **對應專案**：71bk-landing（Public Portfolio）  
> **最後更新**：2025-12-23

---

## 1. 專案資訊

| 項目 | 內容 |
|------|------|
| **專案名稱** | 71bk-landing |
| **專案階段** | M1 初期（Landing Page 開發中） |
| **網域** | `71bk.dev`（待部署） |
| **技術棧** | Vue 3 + Vite + Tailwind CSS v4 + Vue Router + Pinia |

---

## 2. 目錄結構

```
71bk-landing/
├── index.html                  # 入口 HTML（Vite 模板）
├── vite.config.js              # Vite 配置（含 Tailwind 插件）
├── package.json                # 專案設定與依賴
│
├── public/                     # 靜態資源（不經過 Vite 處理）
│
├── src/
│   ├── main.js                 # Vue 應用入口
│   ├── App.vue                 # 根元件
│   │
│   ├── api/                    # API 請求模組（待開發）
│   ├── assets/                 # 靜態資源
│   ├── components/             # Vue 共用元件
│   ├── pinia/                  # Pinia 狀態管理（待開發）
│   │
│   ├── router/                 # Vue Router 路由設定
│   │   ├── index.js            # 主路由配置
│   │   ├── adminRouter.js      # Admin 路由模組
│   │   └── profileRouter.js    # Profile（Public）路由模組
│   │
│   ├── styles/                 # 樣式檔案
│   │   ├── style.css           # 全域樣式（含 Tailwind import）
│   │   ├── theme-color.css     # 主題色彩變數
│   │   └── typography.css      # 字型排版設定
│   │
│   └── views/                  # 頁面視圖
│       ├── admin/              # Admin 後台頁面（7 頁）
│       └── profile/            # Public 前台頁面（7 頁）
│
└── instruction/                # 專案文件
```

---

## 3. 技術棧版本

| 類別 | 套件 | 版本 | 說明 |
|------|------|------|------|
| **核心** | vue | ^3.5.18 | Vue 3 Composition API |
| **路由** | vue-router | ^4.6.4 | SPA 路由 |
| **狀態管理** | pinia | ^3.0.4 | 狀態管理（待整合） |
| **HTTP** | axios | ^1.13.2 | API 請求（待整合） |
| **建置** | vite | ^7.1.2 | 開發與建置工具 |
| **樣式** | tailwindcss | ^4.1.18 | CSS 框架 |

---

## 4. 開發進度

### ✅ 已完成

- [x] Vue 3 + Vite 專案初始化
- [x] 安裝核心依賴（vue-router, pinia, axios, tailwindcss）
- [x] 建立目錄結構（api/, views/, pinia/, components/, router/, styles/）
- [x] **Tailwind CSS v4 整合**（vite.config.js + @import）
- [x] **Vue Router 路由設定**（index.js + adminRouter.js + profileRouter.js）
- [x] **頁面骨架建立**（Admin 7 頁 + Profile 7 頁）
- [x] 主題色彩系統 (`theme-color.css`)
- [x] 字型設定 (`typography.css` + Google Fonts)
- [x] 隱私權政策頁面 (`PrivacyView.vue`)
- [x] 服務條款頁面 (`TermsView.vue`)

### 🔄 進行中

- [ ] Pinia Store 設定
- [ ] 頁面 UI 實作（Home / Projects / About）

### ⏳ 待開發（M1 Roadmap）

- [ ] Home 頁面 UI（Hero + 精選作品）
- [ ] Projects 列表頁 UI（篩選/搜尋）
- [ ] Project Detail 頁面 UI（Case Study）
- [ ] About 頁面 UI
- [ ] Cloudflare Pages 部署設定
- [ ] API 模組與 Mock Data

---

## 5. 與後端協作事項

### 5.1 待後端提供

| 項目 | 狀態 | 說明 |
|------|------|------|
| Public API Endpoints | ⏳ 待開發 | `/api/public/portfolio/projects` |
| API Response 格式 | ⏳ 待確認 | 需確認 JSON 欄位結構 |
| CORS 設定 | ⏳ 待開發 | 允許 `71bk.dev` origin |

### 5.2 前端已準備

| 項目 | 狀態 | 說明 |
|------|------|------|
| API Client 模組 | ⏳ 待整合 | `src/api/` 目錄已建立 |
| Axios 已安裝 | ✅ 完成 | v1.13.2 |
| 環境變數 | ⏳ 待設定 | `VITE_API_BASE_URL` |

---

## 6. 開發指令

```bash
# 安裝依賴
npm install

# 啟動開發伺服器
npm run dev

# 建置生產版本
npm run build

# 預覽生產版本
npm run preview
```

---

## 7. 更新紀錄

| 日期 | 更新內容 |
|------|----------|
| 2025-12-23 | 建立前端進度書，從專案架構書分離前端狀態 |
| 2025-12-23 | 新增 Google Fonts 載入（Inter + Noto Sans TC） |
